// Stack => DS => items/data are stored in LIFO order
// insertion => s.push(25), remove => s.pop(), peek => s.top(), no of elements => s.size(), isEmpty => s.empty()
// usecase :: reverse krna ho
#include <iostream>
#include <stack>
using namespace std;

int main()
{
    // creation
    // stack<int> st;

    // // insertion
    // st.push(10);
    // st.push(20);
    // st.push(30);
    // st.push(40);

    // // remove
    // st.pop();

    // // check elements on top
    // cout << "Element on top is: " << st.top() << endl;

    // // size
    // cout << "Size of Stack is: " << st.size() << endl;

    // if (st.empty())
    // {
    //     cout << "Stack is empty" << endl;
    // }
    // else
    // {
    //     cout << "Stack is not empty" << endl;
    // }


    // print stack elements
    // stack<int> s;

    // s.push(10);
    // s.push(20);
    // s.push(30);
    // s.push(40);

    // while(!s.empty()) {
    //     cout << s.top() << " ";
    //     s.pop();
    // }
    return 0;
}